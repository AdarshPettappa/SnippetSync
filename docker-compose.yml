services:
  backend:
    build:
      context: ./backend
    ports:
      - "8080:8080"
    volumes:
      - snippetsync-data:/app/data

  frontend:
    build:
      context: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - backend

volumes:
  snippetsync-data:
